# CECS343GroupProject
## Restaurant Automation
### Build
1. In the terminal, cd to the ```backend/``` folder
2. Enter: 
    ```
    gradlew build
    ```
### Run

1. In the terminal, cd to the ```backend/``` folder
2. Enter: 
    ```
    gradlew bootRun
    ```
